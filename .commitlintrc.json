{
  "extends": [
    "@uedlinker/commitlint-config"
  ],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "*",
        "root",
        "bin",
        "libs",
        "templates",
        "templates/ui",
        "templates/server",
        "templates/spa",
        "templates/ssr"
      ]
    ]
  }
}
