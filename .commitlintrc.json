{
  "extends": [
    "@uedlinker/commitlint-config"
  ],
  "rules": {
    "scope-enum": [
      2,
      "always",
      [
        "*",
        "root",
        "bin",
        "libs",
        "templates",
        "templates/components",
        "templates/console",
        "templates/server",
        "templates/spa",
        "templates/ssr"
      ]
    ]
  }
}
